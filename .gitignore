node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store

# Next writes both on every build of examples/shop.
.next/
next-env.d.ts

# Benchmark output. Numbers reach the repo through the paper, not as files.
bench/results/
