# Copy to .env.local. Next loads that file on its own; .env.local is gitignored, this one is not.
# See README.md for what each of these does.

ANTHROPIC_API_KEY=
ANTHROPIC_WORKSPACE_ID=
RUDRA_SHOP_MODE=
RUDRA_REPLAY_ONLY=
RUDRA_SHOP_RECORDINGS=
CI=
