# Recordings

One transcript per page the repository makes a claim about — not one per page
anyone happens to load.

Committed here: the answer for `RJ-00001` and shopper `S-0001`, which is the
page `replay-miss.test.ts` guards. That single file is what lets a clone with
no API key render a genuinely generated component, and what arms the guard.

Everything else recorded while browsing with a key set is incidental. It is
model output, it dates, and nothing asserts against it — so it stays out of
git. Delete a stray one rather than committing it; a page with no transcript
falls back, which is the designed behaviour, and re-recording is one page load.
